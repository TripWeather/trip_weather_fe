<%= @user.first_name.titleize %>'s Trips <br>

<% @trips.each do |trip| %>
  <%= link_to trip.name, "/trips/#{trip.id}" %> <br>
  Departure Date: <%= trip.standard_time(trip.departure_date) %> <br>
  Arrival Date: <%= trip.standard_time(trip.arrival_date) %> <br>
  <br>

# trip.find_stops(trip.id) => [stop_1, stop_2].order_by_type_of_stop

  <br>

<% end %>
